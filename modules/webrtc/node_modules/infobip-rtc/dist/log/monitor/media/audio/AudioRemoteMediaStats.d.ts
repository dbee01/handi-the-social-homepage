import { RemoteMediaStats } from "../RemoteMediaStats";
export declare class AudioRemoteMediaStats extends RemoteMediaStats {
    jitter: number;
    concealedSamples: number;
    concealmentEvents: number;
    constructor(id: string);
    get video(): boolean;
    toJSON(): any;
}
