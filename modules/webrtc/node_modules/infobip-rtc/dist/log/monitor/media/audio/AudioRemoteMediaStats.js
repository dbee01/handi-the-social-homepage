import { RemoteMediaStats } from "../RemoteMediaStats";
export class AudioRemoteMediaStats extends RemoteMediaStats {
    constructor(id) {
        super(id);
        this.jitter = 0;
        this.concealedSamples = 0;
        this.concealmentEvents = 0;
    }
    get video() {
        return false;
    }
    toJSON() {
        return {
            ...super.toJSON(),
            jitter: this.jitter,
            concealedSamples: this.concealedSamples,
            concealmentEvents: this.concealmentEvents
        };
    }
}
//# sourceMappingURL=AudioRemoteMediaStats.js.map