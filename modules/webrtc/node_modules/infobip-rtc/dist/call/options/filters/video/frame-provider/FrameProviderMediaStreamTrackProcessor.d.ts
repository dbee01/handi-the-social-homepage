/// <reference types="dom-mediacapture-transform" />
import { FrameListener, FrameProvider } from './FrameProvider';
export declare class FrameProviderMediaStreamTrackProcessor implements FrameProvider {
    private frameListener;
    track: MediaStreamVideoTrack;
    frameSkip: number;
    frameTimeMillis: number;
    sourceFps: number;
    abortController: AbortController;
    constructor(track: MediaStreamVideoTrack, fps: number, frameListener: FrameListener);
    configureFramerate(filterFps: number): void;
    start(): void;
    destroy(): void;
}
