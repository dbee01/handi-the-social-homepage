export class FpsCounter {
    constructor() {
        this.startTimeNs = Date.now();
        this.frameCounter = 0;
        this.lastFps = -1;
    }
    tick() {
        let currentTimeNs = Date.now();
        if (currentTimeNs - this.startTimeNs > 1000000000) {
            this.startTimeNs = currentTimeNs;
            this.lastFps = this.frameCounter;
            this.frameCounter = 1;
        }
        else {
            ++this.frameCounter;
        }
    }
    fps() {
        return this.lastFps;
    }
}
//# sourceMappingURL=FpsCounter.js.map