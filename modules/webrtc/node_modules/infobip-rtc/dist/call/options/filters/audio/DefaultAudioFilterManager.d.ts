import { AudioFilterManager } from "./AudioFilterManager";
import { AudioFilter } from "./AudioFilter";
export declare class DefaultAudioFilterManager implements AudioFilterManager {
    private readonly audioFilter;
    private originalStream;
    private originalTrack;
    private filteredTrack;
    constructor(audioFilter: AudioFilter);
    stop(): Promise<void>;
    getAudioFilter(): AudioFilter;
    start(stream: MediaStream): Promise<void>;
    private setActiveTrack;
}
