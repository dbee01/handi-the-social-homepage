/// <reference types="dom-mediacapture-transform" />
import { RTCVideoFrame } from "../VideoFilter";
export type FrameListener = (frame: RTCVideoFrame) => Promise<void>;
export interface FrameProvider {
    configureFramerate(filterFps: number): void;
    start(): void;
    destroy(): void;
}
export declare function createFrameProvider(track: MediaStreamVideoTrack, fps: number, frameListener: FrameListener): FrameProvider;
