import VideoType from "../util/VideoType";
export interface WebrtcCapturer<T> {
    takeLocalScreenshot(videoType: VideoType): Promise<T>;
    takeRemoteScreenshot(videoType: VideoType): Promise<T>;
}
