import { Endpoint } from "../../util/Endpoint";
import { ViberCallOptions } from "../options/ViberCallOptions";
import { DefaultCall } from "./DefaultCall";
import { ViberCall } from "../ViberCall";
import { InfobipRTC } from "../../InfobipRTC";
import { Logger } from "../../log/Logger";
export declare class DefaultOutgoingViberCall extends DefaultCall implements ViberCall {
    constructor(infobipRtc: InfobipRTC, viberCallOptions: ViberCallOptions, currentUserIdentity: string, phoneNumberDestination: string, callerIdentifier: string, logger: Logger);
    counterpart(): Endpoint;
    options(): ViberCallOptions;
    private mapOptions;
    private initEventHandlers;
}
