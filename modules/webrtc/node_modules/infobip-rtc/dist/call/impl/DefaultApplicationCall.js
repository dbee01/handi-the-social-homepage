import { CameraOrientation } from "../options/CameraOrientation";
import { Audio, Media, Participant, State, Video } from "../../util/Participant";
import { CallStatus } from "../CallStatus";
import MonitoredPeerConnection from "../../log/monitor/MonitoredPeerConnection";
import { MediaUpdateStatus } from "./MediaUpdateStatus";
import { ErrorLog } from "../../log/Log";
import HangupReasonFactory from "./util/HangupReasonFactory";
import HangupStatusFactory from "../util/HangupStatusFactory";
import { MediaType } from "../../log/util/MediaType";
import SimulcastEncodings from "../../util/SimulcastEncodings";
import VideoType from "../../util/VideoType";
import { ApplicationErrorCode } from "../ApplicationErrorCode";
import { WsEvent } from "../ws/WsEvent";
import { ApiEventEmitter } from "../../util/ApiEventEmitter";
import CallProperties from "./CallProperties";
import { EndpointType, PhoneEndpoint, SipEndpoint, ViberEndpoint, WebrtcEndpoint, WebsocketEndpoint, WhatsAppEndpoint } from "../../util/Endpoint";
import { WsAction } from "../ws/WsAction";
import { EmptyAudioStream } from "./EmptyAudioStream";
import { PeerConnectionTag } from "../../log/monitor/media/PeerConnectionTag";
import { DefaultVideoFilterManager } from "../options/filters/video/DefaultVideoFilterManager";
import { NetworkQualityMonitor } from "./network/NetworkQualityMonitor";
import { CallsApiEvent } from "../event/CallsApiEvents";
import VideoRemovalReason from "../../util/VideoRemovalReason";
import { DefaultLocalCapturer } from "./DefaultLocalCapturer";
import { DefaultServerCapturer } from "./DefaultServerCapturer";
import { DefaultDataChannel } from "./DefaultDataChannel";
import { AudioQualityMode } from "../options/AudioQualityMode";
import { RTCMediaDevice } from "../../RTCMediaDevice";
import { BitrateConfig, configureForSending } from "./util/BitrateUtil";
import { ManagedEventEmitter } from "../../util/ManagedEventEmitter";
import { DefaultAudioFilterManager } from "../options/filters/audio/DefaultAudioFilterManager";
import Browser from "../../util/Browser";
import { AdvisorRole, AuditorRole, DefaultRole, ListenerRole, MonitorRole, RoleType } from "../../util/Role";
import { AsyncLock } from "./util/AsyncLock";
import { DefaultAudioStreamEnergyMonitor } from "./AudioStreamEnergyMonitor";
const LOW_DATA_AUDIO_BITRATE = 10000;
var ApplicationCallState;
(function (ApplicationCallState) {
    ApplicationCallState[ApplicationCallState["STANDALONE"] = 0] = "STANDALONE";
    ApplicationCallState[ApplicationCallState["IN_DIALOG"] = 1] = "IN_DIALOG";
    ApplicationCallState[ApplicationCallState["IN_CONFERENCE"] = 2] = "IN_CONFERENCE";
})(ApplicationCallState || (ApplicationCallState = {}));
export class DefaultApplicationCall {
    constructor(eventEmitter, gateway, logger, rtcConfig, device, _callsConfigurationId, applicationCallOptions, currentUserIdentity, token, apiUrl, callId = null) {
        this.gateway = gateway;
        this.logger = logger;
        this.rtcConfig = rtcConfig;
        this.device = device;
        this._callsConfigurationId = _callsConfigurationId;
        this.applicationCallOptions = applicationCallOptions;
        this.currentUserIdentity = currentUserIdentity;
        this.token = token;
        this.apiUrl = apiUrl;
        this.hasRemoteDescription = false;
        this.remoteCandidates = [];
        this.isEarlyMedia = false;
        this.joinedConference = false;
        this.joinedDialog = false;
        this.participants = {};
        this.remoteVideos = {};
        this.videoFilterManager = null;
        this.audioFilterManager = null;
        this.reconnecting = false;
        this._recordingState = {};
        this.cameraVideoEncodings = new Browser().isMobile() ? SimulcastEncodings.cameraEncodingsMobile : SimulcastEncodings.cameraEncodings;
        this.updateCameraStreamLock = new AsyncLock();
        this.eventEmitter = new ManagedEventEmitter(eventEmitter);
        this.callStartTime = new Date();
        this.callStatus = CallStatus.INITIALIZED;
        this.mediaUpdateStatus = MediaUpdateStatus.IDLE;
        this.apiEventEmitter = new ApiEventEmitter(this.logger);
        this.callId = callId || crypto.randomUUID();
        this.localScreenShare = { active: false };
        this.networkQualityMonitor = new NetworkQualityMonitor();
        let videoFilter = this.applicationCallOptions?.videoOptions?.videoFilter;
        if (videoFilter) {
            this.videoFilterManager = new DefaultVideoFilterManager(videoFilter, this.logger, this.callId);
        }
        let audioFilter = this.applicationCallOptions?.audioOptions?.audioFilter;
        if (audioFilter) {
            this.audioFilterManager = new DefaultAudioFilterManager(audioFilter);
        }
        this._audioQualityMode = this.applicationCallOptions?.audioOptions?.audioQualityMode || AudioQualityMode.AUTO;
        if (this.applicationCallOptions?.dataChannel) {
            this.createDataChannel();
        }
        if (this.applicationCallOptions?.audioOptions?.detectTalkingWhileMuted) {
            this.createAudioStreamEnergyMonitor();
        }
        this.initEventHandlers();
        this.handleDeviceChange();
    }
    on(name, handler) {
        if (!Object.values(CallsApiEvent)
            .find(apiEvent => apiEvent === name)) {
            throw new Error(`Unknown event: ${name}!`);
        }
        this.apiEventEmitter.on(name, handler);
    }
    id() {
        return this.callId;
    }
    options() {
        return this.applicationCallOptions;
    }
    customData() {
        return this.applicationCallOptions?.customData;
    }
    callsConfigurationId() {
        return this._callsConfigurationId;
    }
    duration() {
        if (!this.callEstablishTime) {
            return 0;
        }
        return !this.callEndTime ? this.getDurationInSeconds(new Date()) : this.getDurationInSeconds(this.callEndTime);
    }
    endTime() {
        return this.callEndTime;
    }
    establishTime() {
        return this.callEstablishTime;
    }
    hangup() {
        if (this.callStatus !== CallStatus.FINISHED) {
            this.callStatus = CallStatus.FINISHED;
            this.dataChannelCleanup();
            this.scheduleHangup({ callId: this.callId, status: { id: '0', name: 'NO_ERROR', description: 'No Error.' } });
            this.gateway.send({ action: WsAction.HANGUP, callId: this.callId, reason: 'Normal call clearing' });
        }
    }
    startTime() {
        return this.callStartTime;
    }
    status() {
        return this.callStatus;
    }
    async mute(shouldMute) {
        this.validateRoleMediaAllowed();
        this.validateMediaActionAllowed();
        return await this.muteInternal(shouldMute);
    }
    muted() {
        return !this.localAudio.active;
    }
    async sendDTMF(dtmf) {
        if (!dtmf || dtmf.toString().match(/[^0-9*#A-D]/)) {
            throw ApplicationErrorCode.MEDIA_ERROR;
        }
        if (this.callStatus !== CallStatus.ESTABLISHED) {
            throw {
                id: '10103',
                name: 'MEDIA_ERROR',
                description: 'Call not established, action not allowed!'
            };
        }
        this.validateRoleMediaAllowed();
        this.validateMediaActionAllowed();
        this.sendDTMFInfo(dtmf, CallProperties.DTMF_TONE_DURATION);
    }
    pauseIncomingVideo() {
        this.validateMediaActionAllowed();
        if (!this.joinedConference && !this.joinedDialog) {
            return;
        }
        this.gateway.send({
            action: WsAction.PAUSE_INCOMING_VIDEO
        });
    }
    resumeIncomingVideo() {
        this.validateMediaActionAllowed();
        if (!this.joinedConference && !this.joinedDialog) {
            return;
        }
        this.gateway.send({
            action: WsAction.RESUME_INCOMING_VIDEO
        });
    }
    async setAudioInputDevice(deviceId) {
        this.device.setAudioInputDevice(deviceId);
        if (this.callStatus !== CallStatus.ESTABLISHED) {
            return;
        }
        try {
            await this.audioFilterManager?.stop();
            await this.replaceAudioStream();
        }
        catch (error) {
            return this.throwMediaError(error);
        }
    }
    audioFilter() {
        return this.audioFilterManager?.getAudioFilter();
    }
    async setAudioFilter(audioFilter) {
        if (this.audioFilterManager?.getAudioFilter() === audioFilter) {
            return;
        }
        try {
            await this.audioFilterManager?.stop();
            this.audioFilterManager = audioFilter ? new DefaultAudioFilterManager(audioFilter) : null;
            await this.replaceAudioStream();
        }
        catch (error) {
            return this.throwMediaError(error);
        }
    }
    clearAudioFilter() {
        return this.setAudioFilter(null);
    }
    videoFilter() {
        return this.videoFilterManager?.getVideoFilter();
    }
    async setVideoFilter(videoFilter) {
        if (this.videoFilterManager && this.videoFilterManager.getVideoFilter() === videoFilter) {
            return;
        }
        let previousFilter = this.videoFilterManager;
        this.videoFilterManager = videoFilter ? new DefaultVideoFilterManager(videoFilter, this.logger, this.callId) : null;
        await previousFilter?.stop();
        if (this.hasCameraVideo()) {
            await this.videoFilterManager?.start(this.localCameraVideoStream, 0, this.apiEventEmitter);
            await this.updateTransceiver(this.localCameraVideo, this.localCameraVideoStream, this.cameraVideoEncodings);
            this.apiEventEmitter.emit(CallsApiEvent.CAMERA_VIDEO_UPDATED, { stream: this.localCameraVideoStream });
        }
    }
    clearVideoFilter() {
        return this.setVideoFilter(null);
    }
    async setVideoInputDevice(deviceId) {
        this.device.setVideoInputDevice(deviceId);
        if (this.callStatus !== CallStatus.ESTABLISHED) {
            return;
        }
        await this.updateCameraStream(this.cameraOrientation());
    }
    cameraOrientation() {
        return this.device.getCameraOrientation();
    }
    setCameraOrientation(cameraOrientation) {
        return this.updateCameraStream(cameraOrientation, false);
    }
    localCapturer() {
        return this.defaultLocalCaputuer ??= new DefaultLocalCapturer(() => this.localCameraVideoStream, () => this.localScreenShareStream, () => this.videoSubscriberPC?.peerConnection, (identity, videoType) => this.getMidByIdentityAndVideoType(identity, videoType), this.currentUserIdentity);
    }
    serverCapturer() {
        return this.defaultServerCapturer ??= new DefaultServerCapturer(() => this.localCameraVideoStream, () => this.localScreenShareStream, () => this.videoSubscriberPC?.peerConnection, (identity, videoType) => this.getMidByIdentityAndVideoType(identity, videoType), this.currentUserIdentity, this.token, this.apiUrl, this.logger);
    }
    dataChannel() {
        return this._dataChannel;
    }
    setAudioQualityMode(audioQualityMode) {
        this._audioQualityMode = audioQualityMode;
        const senders = this.audioPC.peerConnection.getSenders();
        senders.forEach((sender) => {
            if (sender.track.kind !== 'audio') {
                return;
            }
            const parameters = sender.getParameters();
            parameters.encodings ??= [{}];
            if (parameters.encodings[0]) {
                parameters.encodings[0].priority = 'high';
                parameters.encodings[0].networkPriority = 'high';
            }
            parameters.encodings.forEach(encoding => {
                switch (audioQualityMode) {
                    case AudioQualityMode.LOW_DATA:
                        encoding.maxBitrate = LOW_DATA_AUDIO_BITRATE;
                        break;
                    case AudioQualityMode.AUTO:
                    case AudioQualityMode.HIGH_QUALITY:
                        encoding.maxBitrate = undefined;
                        break;
                }
            });
            sender.setParameters(parameters)
                .catch(err => this.logger.error(`Changing data consumption mode failed: ${err?.message}`));
        });
    }
    audioQualityMode() {
        return this._audioQualityMode;
    }
    hasCameraVideo() {
        return this.localCameraVideo && this.localCameraVideo.active;
    }
    hasScreenShare() {
        return this.localScreenShare && this.localScreenShare.active;
    }
    async stopVideo() {
        this.validateMediaActionAllowed();
        this.validateMediaUpdateStatus();
        return this.stopVideoInternal(VideoRemovalReason.USER_REQUEST);
    }
    async cameraVideo(localVideo) {
        this.validateRoleMediaAllowed();
        this.validateMediaActionAllowed();
        this.validateMediaUpdateStatus();
        if (localVideo !== this.localCameraVideo.active) {
            this.localCameraVideo.active = localVideo;
            return localVideo ? this.addCameraVideo() : this.removeCameraVideo(VideoRemovalReason.USER_REQUEST);
        }
    }
    async screenShare(screenShare) {
        this.validateRoleMediaAllowed();
        this.validateMediaActionAllowed();
        this.validateMediaUpdateStatus();
        if (screenShare !== this.localScreenShare.active) {
            this.localScreenShare.active = screenShare;
            return screenShare ? this.addScreenShareVideo() : this.removeScreenShareVideo(VideoRemovalReason.USER_REQUEST);
        }
    }
    async startScreenShare(displayOptions) {
        this.validateRoleMediaAllowed();
        this.validateMediaActionAllowed();
        this.validateMediaUpdateStatus();
        this.localScreenShare.active = true;
        return this.addScreenShareVideo(displayOptions);
    }
    async stopScreenShare() {
        this.validateMediaActionAllowed();
        this.validateMediaUpdateStatus();
        this.localScreenShare.active = false;
        return this.removeScreenShareVideo(VideoRemovalReason.USER_REQUEST);
    }
    recordingState() {
        return this._recordingState;
    }
    createDataChannel() {
        this._dataChannel = new DefaultDataChannel(this.gateway, this.logger, this.callId, this.currentUserIdentity, identity => this.participants[identity], ice => this.onIceCandidate(WsAction.ICE_CANDIDATE_DATA_CHANNEL, ice), () => this.joinedDialog || this.joinedConference, this.apiEventEmitter, this.conferenceId);
    }
    createAudioPeerConnection() {
        this.audioPC = MonitoredPeerConnection.create(this.rtcConfig, this.callId, PeerConnectionTag.Audio, this.conferenceId || this.dialogId, MediaType.AUDIO, this.logger, this.gateway);
        this.audioPC.peerConnection.onicecandidate = ice => this.onIceCandidate(WsAction.ICE_CANDIDATE, ice);
        this.audioPC.peerConnection.ontrack = event => this.onAudioPcTrack(event);
        this.audioPC.peerConnection.onconnectionstatechange = () => this.onAudioConnectionStateChanged();
        this.audioPC.monitor.onNetworkQualityStatistics((networkQualityStatistics, currentMediaStats) => this.onNetworkQualityStatisticsChanged(networkQualityStatistics, currentMediaStats));
    }
    setLocalAudioStream(audioStream) {
        this.localAudioStream = audioStream;
        let track = this.localAudioStream.getAudioTracks()[0];
        if (!track) {
            this.emptyAudioStream = new EmptyAudioStream();
            return;
        }
        track.enabled = this.localAudio.active;
        if (!this.localAudio.active) {
            this.logger.info("Starting audio stream energy monitor for a call that started muted!", this.callId);
            this.audioStreamEnergyMonitor?.start(this.localAudioStream);
        }
    }
    async setLocalVideoStream(videoStream) {
        this.localCameraVideoStream = videoStream;
        await this.videoFilterManager?.start(this.localCameraVideoStream, 0, this.apiEventEmitter);
        this.mediaUpdateStatus = this.reconnecting ? MediaUpdateStatus.RECONNECTING : MediaUpdateStatus.ADDING_CAMERA_VIDEO;
    }
    createAudioTransceiver() {
        let stream = this.emptyAudioStream ? this.emptyAudioStream.stream() : this.localAudioStream;
        let track = stream.getAudioTracks()[0];
        this.localAudio.transceiver = this.audioPC.peerConnection.addTransceiver(track, {
            streams: [stream],
            direction: 'sendrecv'
        });
    }
    async setLocalDescription(pc, localDescription) {
        await pc.setLocalDescription(localDescription);
        return localDescription;
    }
    setRemoteCandidates() {
        this.hasRemoteDescription = true;
        if (this.remoteCandidates.length > 0) {
            this.remoteCandidates.forEach(candidate => this.addIceCandidate(candidate));
            this.remoteCandidates = [];
        }
    }
    async addCameraVideo() {
        this.mediaUpdateStatus = MediaUpdateStatus.ADDING_CAMERA_VIDEO;
        try {
            this.localCameraVideoStream = await this.getCameraVideoStream(this.cameraOrientation());
            await this.videoFilterManager?.start(this.localCameraVideoStream, 0, this.apiEventEmitter);
        }
        catch (error) {
            this.localCameraVideo.active = false;
            this.handleGetUserMediaError(error, "camera");
            return;
        }
        await this.publishVideo(this.joinedConference || this.joinedDialog);
    }
    async addScreenShareVideo(displayOptions) {
        this.mediaUpdateStatus = MediaUpdateStatus.ADDING_SCREEN_SHARE;
        let isConference = this.joinedConference || this.joinedDialog;
        if (!this.videoPublisherPC) {
            this.createVideoPublisherPC(isConference);
        }
        try {
            this.localScreenShareStream = await this.getScreenShareVideoStream(displayOptions);
        }
        catch (error) {
            this.handleGetUserMediaError(error, "screen-share");
            return;
        }
        try {
            await this.updateTransceiver(this.localScreenShare, this.localScreenShareStream, SimulcastEncodings.screenShareEncodings);
            await this.negotiateVideoPublisher(isConference);
        }
        catch (error) {
            this.handlePublishVideoFlowError(error);
        }
    }
    handleCallFlowError(error, sendHangup = false) {
        this.logger.log(new ErrorLog(this.callId, error));
        if (sendHangup) {
            let reason = HangupReasonFactory.getHangupReason(error);
            this.gateway.send({ action: WsAction.HANGUP, callId: this.callId, reason: reason });
        }
        let hangupStatus = HangupStatusFactory.getApplicationHangupStatus(error);
        this.eventEmitter.emit('call-error', { status: hangupStatus });
    }
    validateMediaUpdateStatus() {
        if (this.mediaUpdateStatus !== MediaUpdateStatus.IDLE && this.mediaUpdateStatus !== MediaUpdateStatus.MEDIA_PENDING) {
            throw new Error("User media already updating.");
        }
    }
    async getLocalMediaStream(audio, video, cameraOrientation, cameraVideoFrameRate) {
        if (!audio && !video) {
            return new MediaStream();
        }
        return await this.device.getLocalStream(audio, video, cameraOrientation, true, true, cameraVideoFrameRate);
    }
    isFinished() {
        return this.callStatus === CallStatus.FINISHED;
    }
    stopAllTracks(mediaStream) {
        mediaStream.getTracks().forEach((track) => track.stop());
    }
    createAudioStreamEnergyMonitor() {
        this.audioStreamEnergyMonitor = DefaultAudioStreamEnergyMonitor.create(() => this.emitTalkingWhileMutedEvent(), this.logger, this.callId);
    }
    audioStreamEnergyMonitorCleanup() {
        if (this.audioStreamEnergyMonitor) {
            this.audioStreamEnergyMonitor.destroy();
            this.audioStreamEnergyMonitor = null;
        }
    }
    dataChannelCleanup() {
        this._dataChannel?.destroy();
    }
    audioCleanup() {
        let totalMediaStats;
        if (this.audioPC) {
            totalMediaStats = this.audioPC.close();
            this.audioPC = null;
            this.hasRemoteDescription = false;
        }
        if (this.emptyAudioStream) {
            this.emptyAudioStream.close();
        }
        if (this.localAudioStream) {
            this.localAudioStream.getAudioTracks().forEach((track) => track.stop());
        }
        return totalMediaStats;
    }
    emitErrorEvent(errorCode) {
        this.apiEventEmitter.emit(CallsApiEvent.ERROR, { errorCode });
    }
    getMidByIdentityAndVideoType(identity, type) {
        return Object.values(this.remoteVideos).find(value => value.participant.endpoint.identifier === identity && value.type === type)?.mid;
    }
    validateMediaActionAllowed() {
        if (this.reconnecting) {
            throw {
                id: '10103',
                name: 'MEDIA_ERROR',
                description: 'Call is reconnecting, action not allowed!'
            };
        }
    }
    validateRoleMediaAllowed() {
        if (this.joinedConference && !this.isRoleMediaAllowed()) {
            throw {
                id: '10103',
                name: 'MEDIA_ERROR',
                description: 'Action not allowed for current role!'
            };
        }
    }
    async muteInternal(shouldMute) {
        if (!this.localAudioStream) {
            throw ApplicationErrorCode.MEDIA_ERROR;
        }
        if (this.emptyAudioStream && !shouldMute) {
            this.emptyAudioStream.close();
            delete this.emptyAudioStream;
            try {
                await this.audioFilterManager?.stop();
                this.localAudioStream = await this.getAudioStream();
                await this.audioFilterManager?.start(this.localAudioStream);
                const track = this.localAudioStream.getAudioTracks()[0];
                await this.replaceTrack(this.localAudio, track);
            }
            catch (error) {
                return this.throwMediaError(error);
            }
        }
        let audioTracks = this.localAudioStream.getAudioTracks();
        if (audioTracks.length === 0) {
            throw ApplicationErrorCode.MEDIA_ERROR;
        }
        this.localAudio.active = !shouldMute;
        let audioTrack = audioTracks[0];
        audioTrack.enabled = this.localAudio.active;
        this.sendMute(shouldMute);
        if (shouldMute) {
            this.logger.info("Starting audio stream energy monitor because call was muted!", this.callId);
            this.audioStreamEnergyMonitor?.start(this.localAudioStream);
        }
        else {
            this.logger.info("Stopping audio stream energy monitor because call was unmuted!", this.callId);
            this.audioStreamEnergyMonitor?.stop();
        }
    }
    async stopVideoInternal(videoRemovalReason) {
        if (this.hasScreenShare() && this.hasCameraVideo()) {
            return await this.removeCameraAndScreenShare(videoRemovalReason);
        }
        if (this.hasScreenShare()) {
            this.localScreenShare.active = false;
            return await this.removeScreenShareVideo(videoRemovalReason);
        }
        if (this.hasCameraVideo()) {
            this.localCameraVideo.active = false;
            return await this.removeCameraVideo(videoRemovalReason);
        }
    }
    async replaceAudioStream() {
        if (this.emptyAudioStream) {
            return;
        }
        this.localAudioStream?.getAudioTracks().forEach((track) => track.stop());
        this.localAudioStream = await this.getAudioStream();
        await this.audioFilterManager?.start(this.localAudioStream);
        const track = this.localAudioStream.getAudioTracks()[0];
        track.enabled = this.localAudio.active;
        if (!track.enabled) {
            this.logger.debug("Starting energy monitor after stream got replaced.", this.callId);
            this.audioStreamEnergyMonitor?.start(this.localAudioStream);
        }
        await this.replaceTrack(this.localAudio, track);
    }
    async getAudioStream() {
        const mediaStream = await this.getLocalMediaStream(true, false);
        return new MediaStream([mediaStream.getAudioTracks()[0]]);
    }
    sendMute(shouldMute) {
        this.gateway.send({
            action: WsAction.MUTE,
            muted: shouldMute,
            callId: this.callId
        });
    }
    async removeScreenShareVideoWithReason(reason) {
        if (this.localScreenShare.active) {
            this.validateMediaUpdateStatus();
            this.localScreenShare.active = false;
            return this.removeScreenShareVideo(reason);
        }
    }
    initEventHandlers() {
        this.eventEmitter.on(WsEvent.TRICKLE_ICE, event => this.handleTrickleIce(event));
        this.eventEmitter.once(WsEvent.RINGING, event => this.ringingHandler(event));
        this.eventEmitter.on(WsEvent.CALL_RESPONSE, event => this.responseHandler(event));
        this.eventEmitter.on(WsEvent.CALL_ACCEPTED, event => this.acceptedHandler());
        this.eventEmitter.once(WsEvent.HANGUP, event => this.hangupHandler(event));
        this.eventEmitter.once(WsEvent.CALL_ERROR, event => this.errorHandler(event));
        this.eventEmitter.on(WsEvent.JOINED_VIDEO_CALL, () => this.videoCallJoinedHandler());
        this.eventEmitter.on(WsEvent.PUBLISHED_VIDEO_CALL, event => this.videoCallPublishedHandler(event));
        this.eventEmitter.on(WsEvent.UNPUBLISHED_VIDEO_CALL, () => this.videoCallUnpublishedHandler());
        this.eventEmitter.on(WsEvent.JOIN_VIDEO_CALL_ERROR, event => this.handleJoinVideoCallError(event));
        this.eventEmitter.on(WsEvent.PUBLISH_VIDEO_CALL_ERROR, event => this.publishVideoCallErrorHandler(event));
        this.eventEmitter.on(WsEvent.CALL_RECORDING_STARTED, event => this.handleCallRecordingStarted(event));
        this.eventEmitter.on(WsEvent.CALL_RECORDING_STOPPED, event => this.handleCallRecordingStopped(event));
        this.eventEmitter.on(WsEvent.JOINED_APPLICATION_CONFERENCE, event => this.handleJoinedApplicationConference(event));
        this.eventEmitter.on(WsEvent.PARTICIPANT_JOINING, event => this.handleParticipantJoining(event));
        this.eventEmitter.on(WsEvent.PARTICIPANT_JOINED, event => this.handleParticipantJoined(event));
        this.eventEmitter.on(WsEvent.PARTICIPANT_MEDIA_CHANGED, event => this.handleParticipantMediaChanged(event));
        this.eventEmitter.on(WsEvent.ROLE_CHANGED, event => this.handleRoleChanged(event));
        this.eventEmitter.on(WsEvent.PARTICIPANT_ROLE_CHANGED, event => this.handleParticipantRoleChanged(event));
        this.eventEmitter.on(WsEvent.STARTED_TALKING, event => this.handleStartedTalking());
        this.eventEmitter.on(WsEvent.STOPPED_TALKING, event => this.handleStoppedTalking());
        this.eventEmitter.on(WsEvent.PARTICIPANT_STARTED_TALKING, event => this.handleParticipantStartedTalking(event));
        this.eventEmitter.on(WsEvent.PARTICIPANT_STOPPED_TALKING, event => this.handleParticipantStoppedTalking(event));
        this.eventEmitter.on(WsEvent.PARTICIPANT_LEFT, event => this.handleParticipantLeft(event));
        this.eventEmitter.on(WsEvent.LEFT_APPLICATION_CONFERENCE, event => this.handleLeftApplicationConference(event));
        this.eventEmitter.on(WsEvent.JOINED_VIDEO_CONFERENCE, () => this.handleJoinedVideoConference());
        this.eventEmitter.on(WsEvent.PUBLISHED_VIDEO_CONFERENCE, event => this.videoConferencePublishedHandler(event));
        this.eventEmitter.on(WsEvent.UNPUBLISHED_VIDEO_CONFERENCE, () => this.videoConferenceUnpublishedHandler());
        this.eventEmitter.on(WsEvent.JOIN_VIDEO_CONFERENCE_ERROR, event => this.handleJoinVideoConferenceError(event));
        this.eventEmitter.on(WsEvent.PUBLISH_VIDEO_CONFERENCE_ERROR, event => this.publishVideoConferenceErrorHandler(event));
        this.eventEmitter.on(WsEvent.SUBSCRIBED_VIDEO, event => this.subscribedVideoHandler(event));
        this.eventEmitter.on(WsEvent.SUBSCRIBE_VIDEO_CONFERENCE_ERROR, event => this.subscribeVideoConferenceError(event));
        this.eventEmitter.on(WsEvent.UPDATED_VIDEO, event => this.updatedVideoHandler(event));
        this.eventEmitter.on(WsEvent.CONFERENCE_RECORDING_STARTED, event => this.handleConferenceRecordingStarted(event));
        this.eventEmitter.on(WsEvent.CONFERENCE_RECORDING_STOPPED, event => this.handleConferenceRecordingStopped(event));
        this.eventEmitter.on(WsEvent.DIALOG_CREATED, event => this.handleDialogCreated(event));
        this.eventEmitter.on(WsEvent.DIALOG_ESTABLISHED, event => this.handleDialogEstablished(event));
        this.eventEmitter.on(WsEvent.DIALOG_FINISHED, event => this.handleDialogFinished(event));
        this.eventEmitter.on(WsEvent.DIALOG_FAILED, event => this.handleDialogFailed(event));
        this.eventEmitter.on(WsEvent.DIALOG_RECORDING_STARTED, event => this.handleDialogRecordingStarted(event));
        this.eventEmitter.on(WsEvent.DIALOG_RECORDING_STOPPED, event => this.handleDialogRecordingStopped(event));
        this.eventEmitter.on(WsEvent.SETUP_DATA_CHANNEL, event => this.handleSetupDataChannel(event));
        this.eventEmitter.on(WsEvent.SETUP_DATA_CHANNEL_ERROR, event => this.setupDataChannelError(event));
        this.eventEmitter.on(WsEvent.PARTICIPANT_NETWORK_QUALITY, event => this.handleParticipantNetworkQuality(event));
        this.eventEmitter.on("receiving_video_media", event => this.videoMediaReceivingHandler());
        this.eventEmitter.on("reconnecting", () => this.handleReconnecting());
        this.eventEmitter.on("reconnected", () => this.handleReconnected());
        this.eventEmitter.on(WsEvent.CALL_RECONNECTED, event => this.handleCallReconnected(event));
        this.eventEmitter.on(WsEvent.PARTICIPANT_DISCONNECTED, event => this.handleParticipantDisconnected(event));
        this.eventEmitter.on(WsEvent.PARTICIPANT_RECONNECTED, event => this.handleParticipantReconnected(event));
        this.eventEmitter.on(WsEvent.MESSAGE_RECEIVED, event => this.handleMessageReceived(event));
        this.eventEmitter.on(WsEvent.MACHINE_DETECTION_FINISHED, event => this.handleMachineDetectionFinished(event));
        this.eventEmitter.on(WsEvent.MACHINE_DETECTION_FAILED, event => this.handleMachineDetectionFailed(event));
    }
    handleDeviceChange() {
        navigator.mediaDevices.ondevicechange = async () => {
            let currentlyUsedDeviceId = this.device.getAudioInputDevice() || "default";
            if (currentlyUsedDeviceId === "default") {
                this.switchToDefaultDevice();
            }
            else {
                let shouldChange = await this.device.audioInputDeviceShouldChange();
                if (shouldChange) {
                    this.switchToDefaultDevice();
                }
            }
        };
    }
    switchToDefaultDevice() {
        this.setAudioInputDevice("default")
            .catch(err => {
            this.logger.error(`Switching audio input device failed (${err?.message})`, this.callId);
        });
    }
    updateVideoBitrate() {
        if (this.videoPublisherPC.peerConnection.connectionState !== 'connected') {
            return;
        }
        let desiredBitrate = [
            this.hasCameraVideo() ? BitrateConfig.CAMERA : 0,
            this.hasScreenShare() ? BitrateConfig.SCREENSHARE : 0
        ].reduce((prev, curr) => prev + curr, 0);
        configureForSending(this.videoPublisherPC.peerConnection, "video", desiredBitrate, this.logger);
    }
    onVideoPublisherConnectionStateChanged() {
        this.updateVideoBitrate();
    }
    onAudioConnectionStateChanged() {
        if (this.audioPC.peerConnection.connectionState === 'connected') {
            this.setAudioQualityMode(this._audioQualityMode);
        }
    }
    ringingHandler(event) {
        this.callStatus = CallStatus.RINGING;
        this.updateCustomData(event);
        this.apiEventEmitter.emit(CallsApiEvent.RINGING, {});
    }
    async responseHandler(event) {
        this.isEarlyMedia = event.isEarlyMedia;
        try {
            await this.audioPC.peerConnection.setRemoteDescription(event.description);
            this.setRemoteCandidates();
        }
        catch (error) {
            this.handleCallFlowError(error, true);
        }
    }
    acceptedHandler() {
        if (this.callStatus === CallStatus.ESTABLISHED) {
            return;
        }
        this.callStatus = CallStatus.ESTABLISHED;
        this.callEstablishTime = new Date();
        this.apiEventEmitter.emit(CallsApiEvent.ESTABLISHED, {
            stream: this.remoteAudioStream
        });
    }
    videoCallJoinedHandler() {
        if (this.joinedConference || this.joinedDialog) {
            return;
        }
        if (this.hasCameraVideo() || this.hasScreenShare()) {
            this.publishVideo(false);
        }
    }
    changeMonitorConferenceId(conferenceId) {
        if (this.audioPC) {
            this.audioPC.monitor.conferenceId = conferenceId;
        }
        if (this.videoPublisherPC) {
            this.videoPublisherPC.monitor.conferenceId = conferenceId;
        }
        if (this.videoSubscriberPC) {
            this.videoSubscriberPC.monitor.conferenceId = conferenceId;
        }
    }
    async handleSetupDataChannel(event) {
        this._dataChannel?.initialize(event, this.rtcConfig);
    }
    handleJoinedApplicationConference(event) {
        this.joinedConference = true;
        this.conferenceId = event.id;
        this.changeMonitorConferenceId(event.id);
        this.participants = this.createParticipantMap(event.participants);
        this._recordingState.conferenceRecording = event.recordingType;
        if (!this.isRoleMediaAllowed()) {
            this.disableMedia();
        }
        this.apiEventEmitter.emit(CallsApiEvent.CONFERENCE_JOINED, {
            id: event.id,
            name: event.name,
            participants: Object.values(this.participants),
            recordingType: event.recordingType
        });
    }
    handleDialogCreated(event) {
        this.joinedDialog = true;
        this.dialogId = event.id;
        this.changeMonitorConferenceId(event.id);
        this.participants = this.createParticipantMap(event.participants);
        const participant = Object.values(this.participants)
            .find(participant => participant.endpoint.identifier !== this.currentUserIdentity);
        this._recordingState.dialogRecording = event.recordingType;
        this.apiEventEmitter.emit(CallsApiEvent.DIALOG_JOINED, {
            id: event.id,
            remote: participant,
            recordingType: event.recordingType
        });
    }
    createParticipantMap(participants) {
        return participants.reduce((map, p) => {
            let participant = this.loadParticipant(p);
            let identifier = participant.endpoint.identifier;
            map[identifier] = participant;
            return map;
        }, {});
    }
    handleDialogEstablished(event) {
        this.participants = this.createParticipantMap(event.participants);
        const participant = Object.values(this.participants)
            .find(participant => participant.endpoint.identifier !== this.currentUserIdentity);
        this._recordingState.dialogRecording = event.recordingType;
        if (!this.joinedDialog) {
            this.joinedDialog = true;
            this.dialogId = event.id;
            this.changeMonitorConferenceId(event.id);
            this.apiEventEmitter.emit(CallsApiEvent.DIALOG_JOINED, {
                id: event.id,
                remote: participant,
                recordingType: event.recordingType
            });
        }
        if (participant.media.audio.muted) {
            this.apiEventEmitter.emit(CallsApiEvent.PARTICIPANT_MUTED, { participant: participant });
        }
    }
    handleParticipantJoining(event) {
        if (this.joinedConference) {
            const participant = this.loadParticipant(event.participant);
            const identifier = participant.endpoint.identifier;
            if (!this.participants[identifier]) {
                this.participants[identifier] = participant;
                this.apiEventEmitter.emit(CallsApiEvent.PARTICIPANT_JOINING, { participant: participant });
            }
        }
    }
    handleParticipantJoined(event) {
        if (this.joinedConference) {
            const participant = this.loadParticipant(event.participant);
            const identifier = participant.endpoint.identifier;
            if (!this.participants[identifier] || this.participants[identifier].state === State.JOINING) {
                this.participants[identifier] = participant;
                this._dataChannel?.addParticipant(identifier);
                this.apiEventEmitter.emit(CallsApiEvent.PARTICIPANT_JOINED, { participant: participant });
            }
        }
    }
    handleParticipantMediaChanged(event) {
        if (this.joinedConference || this.joinedDialog) {
            const endpoint = this.getEndpoint(event.endpoint);
            const identifier = endpoint.identifier;
            if (this.participants[identifier]) {
                let participant = this.participants[identifier];
                if (event.media.audio?.muted !== undefined) {
                    participant.media.audio.muted = event.media.audio.muted;
                    this.emitMutedEvent(participant);
                }
                if (event.media.audio?.deaf !== undefined && !this.joinedDialog) {
                    participant.media.audio.deaf = event.media.audio.deaf;
                    this.emitDeafEvent(participant);
                }
                if (event.media.video?.blind !== undefined && !this.joinedDialog) {
                    participant.media.video.blind = event.media.video.blind;
                    this.emitBlindEvent(participant);
                }
            }
        }
    }
    handleRoleChanged(event) {
        const role = this.getRole(event.role);
        const localParticipant = this.participants[this.currentUserIdentity];
        if (localParticipant) {
            localParticipant.role = role;
        }
        if (!this.isRoleMediaAllowed()) {
            this.disableMedia();
        }
        this.apiEventEmitter.emit(CallsApiEvent.ROLE_CHANGED, { role });
    }
    isRoleMediaAllowed() {
        const localRoleType = this.participants[this.currentUserIdentity]?.role?.type;
        return !localRoleType || [RoleType.DEFAULT, RoleType.ADVISOR].includes(localRoleType);
    }
    async disableMedia() {
        let actions = [];
        if (!this.muted()) {
            actions.push(this.muteInternal(true));
        }
        if (this.hasCameraVideo() || this.hasScreenShare()) {
            actions.push(this.stopVideoInternal(VideoRemovalReason.ROLE_CHANGED));
        }
        await Promise.all(actions);
    }
    handleParticipantRoleChanged(event) {
        if (!this.joinedConference) {
            return;
        }
        const participant = this.loadParticipant(event.participant);
        const identifier = participant.endpoint.identifier;
        this.participants[identifier] = participant;
        this.emitParticipantRoleChanged(participant);
    }
    emitDeafEvent(participant) {
        if (participant.media.audio.deaf) {
            this.apiEventEmitter.emit(CallsApiEvent.PARTICIPANT_DEAF, { participant: participant });
        }
        else {
            this.apiEventEmitter.emit(CallsApiEvent.PARTICIPANT_UNDEAF, { participant: participant });
        }
    }
    emitMutedEvent(participant) {
        if (participant.media.audio.muted) {
            this.apiEventEmitter.emit(CallsApiEvent.PARTICIPANT_MUTED, { participant: participant });
        }
        else {
            this.apiEventEmitter.emit(CallsApiEvent.PARTICIPANT_UNMUTED, { participant: participant });
        }
    }
    emitBlindEvent(participant) {
        if (participant.media.video.blind) {
            this.apiEventEmitter.emit(CallsApiEvent.PARTICIPANT_BLINDED, { participant: participant });
        }
        else {
            this.apiEventEmitter.emit(CallsApiEvent.PARTICIPANT_UNBLINDED, { participant: participant });
        }
    }
    emitDisconnectedEvent(participant) {
        if (participant.disconnected) {
            this.apiEventEmitter.emit(CallsApiEvent.PARTICIPANT_DISCONNECTED, { participant: participant });
        }
        else {
            this.apiEventEmitter.emit(CallsApiEvent.PARTICIPANT_RECONNECTED, { participant: participant });
        }
    }
    emitParticipantRoleChanged(participant) {
        this.apiEventEmitter.emit(CallsApiEvent.PARTICIPANT_ROLE_CHANGED, { participant: participant });
    }
    handleStartedTalking() {
        if (this.joinedConference) {
            const localParticipant = this.participants[this.currentUserIdentity];
            if (localParticipant) {
                localParticipant.media.audio.talking = true;
            }
            this.emitTalkingEvent(true);
        }
    }
    handleStoppedTalking() {
        if (this.joinedConference) {
            const localParticipant = this.participants[this.currentUserIdentity];
            if (localParticipant) {
                localParticipant.media.audio.talking = false;
            }
            this.emitTalkingEvent(false);
        }
    }
    handleParticipantStartedTalking(event) {
        if (this.joinedConference) {
            const endpoint = this.getEndpoint(event.endpoint);
            const identifier = endpoint.identifier;
            if (this.participants[identifier]) {
                let participant = this.participants[identifier];
                participant.media.audio.talking = true;
                this.emitParticipantTalkingEvent(participant, true);
            }
        }
    }
    handleParticipantStoppedTalking(event) {
        if (this.joinedConference) {
            const endpoint = this.getEndpoint(event.endpoint);
            const identifier = endpoint.identifier;
            if (this.participants[identifier]) {
                let participant = this.participants[identifier];
                participant.media.audio.talking = false;
                this.emitParticipantTalkingEvent(participant, false);
            }
        }
    }
    emitTalkingEvent(isTalking) {
        if (isTalking) {
            this.apiEventEmitter.emit(CallsApiEvent.STARTED_TALKING, {});
        }
        else {
            this.apiEventEmitter.emit(CallsApiEvent.STOPPED_TALKING, {});
        }
    }
    emitParticipantTalkingEvent(participant, isTalking) {
        if (isTalking) {
            this.apiEventEmitter.emit(CallsApiEvent.PARTICIPANT_STARTED_TALKING, { participant: participant });
        }
        else {
            this.apiEventEmitter.emit(CallsApiEvent.PARTICIPANT_STOPPED_TALKING, { participant: participant });
        }
    }
    handleParticipantLeft(event) {
        if (this.joinedConference) {
            const participant = this.loadParticipant(event.participant);
            const identifier = participant.endpoint.identifier;
            delete this.participants[identifier];
            this._dataChannel?.removeParticipant(identifier);
            this.apiEventEmitter.emit(CallsApiEvent.PARTICIPANT_LEFT, { participant: participant });
        }
    }
    async handleJoinedVideoConference() {
        this.videoPublisherCleanup(true);
        if (this.hasCameraVideo() || this.hasScreenShare()) {
            if (this.mediaUpdateStatus === MediaUpdateStatus.IDLE) {
                this.mediaUpdateStatus = MediaUpdateStatus.MIGRATING;
            }
            await this.migrateLocalVideos();
            try {
                await this.negotiateVideoPublisher(true);
            }
            catch (error) {
                this.handlePublishVideoFlowError(error);
            }
        }
    }
    subscribedVideoHandler(event) {
        this.remoteVideos = this.mapStreamEvent(event.streams);
        this.createVideoSubscriberPC();
        this.videoSubscriberPC.peerConnection.setRemoteDescription(event.description)
            .then(() => this.videoSubscriberPC.peerConnection.createAnswer())
            .then(answer => this.setLocalDescription(this.videoSubscriberPC.peerConnection, answer))
            .then(answer => {
            this.gateway.send({
                action: WsAction.START_VIDEO_CONFERENCE,
                description: answer
            });
        })
            .catch(error => this.handleSubscribeVideoFlowError(error));
    }
    handleSubscribeVideoFlowError(error) {
        this.logger.error(`Subscribe video flow error occurred: ${error}`, this.callId);
        this.videoSubscriberCleanup();
        let errorCode = HangupStatusFactory.getApplicationHangupStatus(error);
        this.apiEventEmitter.emit(CallsApiEvent.ERROR, { errorCode });
    }
    updatedVideoHandler(event) {
        if (event.streams) {
            Object.keys(this.remoteVideos)
                .filter(mid => !event.streams[mid])
                .forEach(mid => {
                let remoteVideo = this.remoteVideos[mid];
                delete this.remoteVideos[mid];
                this.emitParticipantVideoRemovedEvent(remoteVideo.type, remoteVideo.participant);
            });
            this.remoteVideos = this.mapStreamEvent(event.streams);
        }
        else {
            this.videoSubscriberPC.peerConnection.restartIce();
        }
        this.videoSubscriberPC.peerConnection.setRemoteDescription(event.description)
            .then(() => this.videoSubscriberPC.peerConnection.createAnswer())
            .then(answer => this.setLocalDescription(this.videoSubscriberPC.peerConnection, answer))
            .then(answer => {
            this.gateway.send({
                action: WsAction.START_VIDEO_CONFERENCE,
                description: answer
            });
        })
            .catch(error => this.handleSubscribeVideoFlowError(error));
    }
    emitParticipantVideoRemovedEvent(type, participant) {
        if (type === VideoType.CAMERA) {
            this.apiEventEmitter.emit(CallsApiEvent.PARTICIPANT_CAMERA_VIDEO_REMOVED, { participant: participant });
        }
        else if (type === VideoType.SCREENSHARE) {
            this.apiEventEmitter.emit(CallsApiEvent.PARTICIPANT_SCREEN_SHARE_REMOVED, { participant: participant });
        }
    }
    async updateCameraStream(cameraOrientation = CameraOrientation.FRONT, useExactDevice = true) {
        return this.updateCameraStreamLock.withLock(async () => {
            if (!this.hasCameraVideo()) {
                throw new Error("Camera video is not enabled.");
            }
            this.localCameraVideoStream?.getVideoTracks().forEach(track => track.stop());
            try {
                await this.videoFilterManager?.stop();
                const stream = await this.getCameraVideoStream(cameraOrientation, useExactDevice);
                this.localCameraVideoStream?.getVideoTracks().forEach(track => track.stop());
                this.localCameraVideoStream = stream;
                this.apiEventEmitter.emit(CallsApiEvent.CAMERA_VIDEO_UPDATED, { stream: stream });
                await this.videoFilterManager?.start(stream, 0, this.apiEventEmitter);
                return this.replaceTrack(this.localCameraVideo, stream.getVideoTracks()[0]);
            }
            catch (error) {
                this.throwMediaError(error);
            }
        });
    }
    async handleLeftApplicationConference(event) {
        this.joinedConference = false;
        this.conferenceId = null;
        this.changeMonitorConferenceId(null);
        this.participants = {};
        this.remoteVideos = {};
        this.videoPublisherCleanup(true);
        this.videoSubscriberCleanup();
        this.dataChannelCleanup();
        this._recordingState.conferenceRecording = "UNDEFINED";
        if (this.hasCameraVideo() || this.hasScreenShare()) {
            this.mediaUpdateStatus = MediaUpdateStatus.MIGRATING;
            await this.migrateLocalVideos();
        }
        this.apiEventEmitter.emit(CallsApiEvent.CONFERENCE_LEFT, { errorCode: event.status });
    }
    async handleDialogFinished(event) {
        this.joinedDialog = false;
        this.dialogId = null;
        this.changeMonitorConferenceId(null);
        this.participants = {};
        this.remoteVideos = {};
        this.videoPublisherCleanup(true);
        this.videoSubscriberCleanup();
        this.dataChannelCleanup();
        this._recordingState.dialogRecording = "UNDEFINED";
        if (this.hasCameraVideo() || this.hasScreenShare()) {
            this.mediaUpdateStatus = MediaUpdateStatus.MIGRATING;
            await this.migrateLocalVideos();
        }
        this.apiEventEmitter.emit(CallsApiEvent.DIALOG_LEFT, { errorCode: event.status });
    }
    async handleReconnecting() {
        if (this.callStatus !== CallStatus.ESTABLISHED) {
            this.logger.warn("Reconnect failed: call is not established");
            this.hangupHandler({ callId: this.callId, status: ApplicationErrorCode.NETWORK_ERROR });
            return;
        }
        if (!this.applicationCallOptions?.autoReconnect) {
            this.logger.debug("Websocket is reconnecting, but the active call doesn't support reconnect. Hanging up...");
            this.hangupHandler({ callId: this.callId, status: ApplicationErrorCode.NETWORK_ERROR });
            return;
        }
        this.reconnecting = true;
        this.apiEventEmitter.emit(CallsApiEvent.RECONNECTING, {});
    }
    async handleReconnected() {
        this.logger.info(`Reconnecting call ${this.callId}, restarting ICE...`);
        const audioOffer = await this.audioPC?.restartIce();
        this.gateway.send({
            action: WsAction.RECONNECT_APPLICATION_CALL,
            callId: this.callId,
            audioOffer,
        });
    }
    async handleCallReconnected(event) {
        this.callStatus = CallStatus.ESTABLISHED;
        await this.audioPC.peerConnection.setRemoteDescription(event.description);
        await this.handleApplicationCallChanges(event);
        let stateBeforeReconnect = this.resolveCurrentCallState();
        let stateAfterReconnect = this.resolveCallStateAfterReconnect(event);
        if (stateBeforeReconnect === stateAfterReconnect) {
            await this.handleCallReconnectedToSameState(stateAfterReconnect, event);
            await this.renegotiateMedia(true);
        }
        else {
            await this.handleCallReconnectedToNewState(stateBeforeReconnect, stateAfterReconnect, event);
            await this.renegotiateMedia(false);
        }
        this.reconnecting = false;
        this.apiEventEmitter.emit(CallsApiEvent.RECONNECTED, {});
    }
    async handleDialogFailed(event) {
        this.joinedDialog = false;
        this.dialogId = null;
        this.changeMonitorConferenceId(null);
        this.participants = {};
        this.remoteVideos = {};
        this.videoPublisherCleanup(true);
        this.videoSubscriberCleanup();
        this.dataChannelCleanup();
        if (this.hasCameraVideo() || this.hasScreenShare()) {
            this.mediaUpdateStatus = MediaUpdateStatus.MIGRATING;
            await this.migrateLocalVideos();
        }
        this.apiEventEmitter.emit(CallsApiEvent.DIALOG_LEFT, { errorCode: event.status });
    }
    videoSubscriberCleanup() {
        if (this.videoSubscriberPC) {
            this.videoSubscriberPC.close();
            this.videoSubscriberPC = null;
        }
    }
    handleJoinVideoCallError(event) {
        if (this.joinedConference || this.joinedDialog) {
            return;
        }
        this.apiEventEmitter.emit(CallsApiEvent.ERROR, { errorCode: event.status });
        this.videoSubscriberCleanup();
        this.videoPublisherCleanup();
    }
    handleJoinVideoConferenceError(event) {
        if (!this.joinedConference && !this.joinedDialog) {
            return;
        }
        this.apiEventEmitter.emit(CallsApiEvent.ERROR, { errorCode: event.status });
        this.dataChannelCleanup();
        this.videoSubscriberCleanup();
        this.videoPublisherCleanup();
    }
    handleCallRecordingStarted(event) {
        this._recordingState.callRecording = event.recordingType;
        this.apiEventEmitter.emit(CallsApiEvent.CALL_RECORDING_STARTED, { recordingType: event.recordingType });
    }
    handleCallRecordingStopped(event) {
        this._recordingState.callRecording = "UNDEFINED";
        this.apiEventEmitter.emit(CallsApiEvent.CALL_RECORDING_STOPPED, {});
    }
    handleDialogRecordingStarted(event) {
        this._recordingState.dialogRecording = event.recordingType;
        this.apiEventEmitter.emit(CallsApiEvent.DIALOG_RECORDING_STARTED, { recordingType: event.recordingType });
    }
    handleDialogRecordingStopped(event) {
        this._recordingState.dialogRecording = "UNDEFINED";
        this.apiEventEmitter.emit(CallsApiEvent.DIALOG_RECORDING_STOPPED, {});
    }
    handleConferenceRecordingStarted(event) {
        this._recordingState.conferenceRecording = event.recordingType;
        this.apiEventEmitter.emit(CallsApiEvent.CONFERENCE_RECORDING_STARTED, { recordingType: event.recordingType });
    }
    handleConferenceRecordingStopped(event) {
        this._recordingState.conferenceRecording = "UNDEFINED";
        this.apiEventEmitter.emit(CallsApiEvent.CONFERENCE_RECORDING_STOPPED, {});
    }
    subscribeVideoConferenceError(event) {
        this.apiEventEmitter.emit(CallsApiEvent.ERROR, { errorCode: event.status });
        this.videoSubscriberCleanup();
    }
    setupDataChannelError(event) {
        this.apiEventEmitter.emit(CallsApiEvent.ERROR, { errorCode: event.status });
        this.dataChannelCleanup();
    }
    handleParticipantNetworkQuality(event) {
        const participant = this.participants[event.endpoint.identity];
        this.apiEventEmitter.emit(CallsApiEvent.PARTICIPANT_NETWORK_QUALITY_CHANGED, {
            networkQuality: event.networkQuality,
            participant
        });
    }
    handleParticipantDisconnected(event) {
        const participant = this.updateParticipant(event.endpoint.identity, (participant) => participant.disconnected = true);
        this.apiEventEmitter.emit(CallsApiEvent.PARTICIPANT_DISCONNECTED, {
            participant
        });
    }
    handleParticipantReconnected(event) {
        const participant = this.updateParticipant(event.endpoint.identity, (participant) => participant.disconnected = false);
        this.apiEventEmitter.emit(CallsApiEvent.PARTICIPANT_RECONNECTED, {
            participant
        });
    }
    updateParticipant(identity, updateCallback) {
        const participant = this.participants[identity];
        updateCallback(participant);
        return participant;
    }
    mapStreamEvent(streams) {
        return Object.keys(streams).reduce((videos, mid) => ({
            ...videos,
            [mid]: {
                mid: mid,
                participant: this.participants[streams[mid].participant],
                type: VideoType[streams[mid].type]
            }
        }), {});
    }
    createVideoSubscriberPC() {
        this.videoSubscriberPC = MonitoredPeerConnection.create(this.rtcConfig, this.callId, PeerConnectionTag.VideoSubscription, this.conferenceId || this.dialogId, MediaType.VIDEO, this.logger, this.gateway);
        this.videoSubscriberPC.peerConnection.onicecandidate = ice => this.onIceCandidate(WsAction.ICE_CANDIDATE_VIDEO_CONFERENCE, ice, 'subscriber');
        this.videoSubscriberPC.peerConnection.ontrack = event => this.onVideoPcTrack(event);
    }
    onVideoPcTrack(event) {
        if (event.track.kind === 'video') {
            let remoteVideo = this.remoteVideos[event.transceiver.mid];
            if (remoteVideo.type === VideoType.SCREENSHARE && this.hasScreenShare()) {
                this.removeScreenShareVideoWithReason(VideoRemovalReason.ACTIVE_PRESENTER_CHANGED);
            }
            if (remoteVideo.type === VideoType.CAMERA) {
                if (!remoteVideo.participant) {
                    this.logger.warn("Error while emitting participant-camera-video-added: participant isn't present", this.callId);
                    return;
                }
                this.apiEventEmitter.emit(CallsApiEvent.PARTICIPANT_CAMERA_VIDEO_ADDED, {
                    participant: remoteVideo.participant,
                    stream: new MediaStream([event.track])
                });
            }
            else {
                this.apiEventEmitter.emit(CallsApiEvent.PARTICIPANT_SCREEN_SHARE_ADDED, {
                    participant: remoteVideo.participant,
                    stream: new MediaStream([event.track])
                });
            }
        }
    }
    videoMediaReceivingHandler() {
        if (this.mediaUpdateStatus === MediaUpdateStatus.MEDIA_PENDING) {
            this.mediaUpdateStatus = MediaUpdateStatus.IDLE;
        }
    }
    videoCallPublishedHandler(event) {
        if (this.joinedConference || this.joinedDialog) {
            return;
        }
        this.videoPublisherPC.peerConnection.setRemoteDescription(event.description)
            .then(() => {
            this.emitVideoAddedEvent();
            this.mediaUpdateStatus = MediaUpdateStatus.MEDIA_PENDING;
        })
            .then(() => this.updateVideoBitrate())
            .catch(error => this.handlePublishVideoFlowError(error));
    }
    videoConferencePublishedHandler(event) {
        if (!this.joinedConference && !this.joinedDialog) {
            return;
        }
        this.videoPublisherPC.peerConnection.setRemoteDescription(event.description)
            .then(() => {
            this.emitVideoAddedEvent();
            this.mediaUpdateStatus = MediaUpdateStatus.MEDIA_PENDING;
        })
            .then(() => this.updateVideoBitrate())
            .catch(error => this.handlePublishVideoFlowError(error));
    }
    publishVideoCallErrorHandler(event) {
        if (this.joinedConference || this.joinedDialog) {
            return;
        }
        this.videoPublisherCleanup();
        this.apiEventEmitter.emit(CallsApiEvent.ERROR, { errorCode: event.status });
    }
    publishVideoConferenceErrorHandler(event) {
        if (!this.joinedConference && !this.joinedDialog) {
            return;
        }
        this.videoPublisherCleanup();
        this.apiEventEmitter.emit(CallsApiEvent.ERROR, { errorCode: event.status });
    }
    videoCallUnpublishedHandler() {
        if (this.joinedConference || this.joinedDialog) {
            return;
        }
        this.emitVideoRemovedEvent();
        this.videoPublisherCleanup();
    }
    videoConferenceUnpublishedHandler() {
        if (!this.joinedConference && !this.joinedDialog) {
            return;
        }
        this.emitVideoRemovedEvent();
        this.videoPublisherCleanup();
    }
    hangupHandler(event) {
        this.callStatus = CallStatus.FINISHED;
        this.callEndTime = new Date();
        this.eventEmitter.emit('call_finished', {});
        let totalMediaStats = this.audioCleanup();
        this.apiEventEmitter.emit(CallsApiEvent.HANGUP, {
            errorCode: event.status,
            totalMediaStats: totalMediaStats
        });
        this.cleanup();
    }
    errorHandler(event) {
        this.logger.log(new ErrorLog(this.callId, event.status));
        this.callStatus = CallStatus.FINISHED;
        this.callEndTime = new Date();
        this.eventEmitter.emit('call_finished', {});
        let totalMediaStats = this.audioCleanup();
        this.apiEventEmitter.emit(CallsApiEvent.HANGUP, {
            errorCode: event.status,
            totalMediaStats: totalMediaStats
        });
        this.cleanup();
    }
    async cleanup() {
        navigator.mediaDevices.ondevicechange = null;
        await this.videoFilterManager?.stop();
        if (this.audioFilterManager) {
            await this.audioFilterManager.stop();
            this.localAudioStream?.getAudioTracks().forEach((track) => track.stop());
        }
        this.dataChannelCleanup();
        this.videoPublisherCleanup();
        this.videoSubscriberCleanup();
        this.audioStreamEnergyMonitorCleanup();
        this.eventEmitter.releaseSubscriptions();
        Object.values(CallsApiEvent).forEach(eventName => this.apiEventEmitter.removeAllListeners(eventName));
    }
    handleTrickleIce(event) {
        if (!this.hasRemoteDescription) {
            this.remoteCandidates.push(event.candidate);
            return;
        }
        this.addIceCandidate(event.candidate);
    }
    addIceCandidate(candidate) {
        let iceCandidate = candidate.completed ? null : candidate;
        this.audioPC.peerConnection.addIceCandidate(iceCandidate)
            .catch(error => this.logger.log(new ErrorLog(this.callId, `Error adding ice candidate ${candidate}: ${error}`)));
    }
    async publishVideo(isConference) {
        if (!this.videoPublisherPC) {
            this.createVideoPublisherPC(isConference);
        }
        try {
            if (this.hasCameraVideo()) {
                await this.updateTransceiver(this.localCameraVideo, this.localCameraVideoStream, this.cameraVideoEncodings);
            }
            await this.negotiateVideoPublisher(isConference);
        }
        catch (error) {
            this.handlePublishVideoFlowError(error);
        }
    }
    async updateTransceiver(media, stream, encodings) {
        let track = stream.getVideoTracks()[0];
        if (media.transceiver) {
            await media.transceiver.sender.replaceTrack(track);
            media.transceiver.direction = 'sendonly';
        }
        else {
            this.addTransceiver(media, track, stream, encodings);
        }
    }
    async migrateLocalVideos() {
        this.createVideoPublisherPC(this.joinedConference || this.joinedDialog);
        if (this.hasCameraVideo()) {
            this.addTransceiver(this.localCameraVideo, this.localCameraVideoStream.getVideoTracks()[0], this.localCameraVideoStream, this.cameraVideoEncodings);
        }
        if (this.hasScreenShare()) {
            this.addTransceiver(this.localScreenShare, this.localScreenShareStream.getVideoTracks()[0], this.localScreenShareStream, SimulcastEncodings.screenShareEncodings);
        }
    }
    addTransceiver(media, track, stream, encodings) {
        if (media.transceiver) {
            const oldTrack = media.transceiver?.sender?.track;
            if (oldTrack && oldTrack !== track) {
                oldTrack.stop();
            }
        }
        media.transceiver = this.videoPublisherPC.peerConnection.addTransceiver(track, {
            streams: [stream],
            direction: 'sendonly',
            sendEncodings: encodings
        });
    }
    async negotiateVideoPublisher(isConference) {
        let offer = await this.videoPublisherPC.peerConnection.createOffer();
        await this.setLocalDescription(this.videoPublisherPC.peerConnection, offer);
        let labels = [];
        if (this.localCameraVideo.active) {
            labels.push({ mid: this.localCameraVideo.transceiver.mid, description: VideoType.CAMERA });
        }
        if (this.localScreenShare.active) {
            labels.push({ mid: this.localScreenShare.transceiver.mid, description: VideoType.SCREENSHARE });
        }
        let action = isConference ? WsAction.PUBLISH_VIDEO_CONFERENCE : WsAction.PUBLISH_VIDEO_CALL;
        this.gateway.send({
            action: action,
            callId: this.callId,
            description: offer,
            labels: labels
        });
    }
    async removeCameraVideo(reason) {
        if (!this.localCameraVideo.transceiver) {
            return;
        }
        let previousMediaUpdateStatus = this.mediaUpdateStatus;
        this.mediaUpdateStatus = MediaUpdateStatus.REMOVING_CAMERA_VIDEO;
        this.cameraVideoRemovalReason = reason;
        await this.disableLocalMedia(this.localCameraVideo, previousMediaUpdateStatus);
        await this.videoFilterManager?.stop();
    }
    async removeCameraAndScreenShare(videoRemovalReason) {
        if (!this.localCameraVideo.transceiver || !this.localScreenShare.transceiver) {
            this.logger.warn("Remove camera and screen share called in invalid state.");
            return;
        }
        let previousUpdateStatus = this.mediaUpdateStatus;
        this.mediaUpdateStatus = MediaUpdateStatus.REMOVING_VIDEO;
        this.cameraVideoRemovalReason = videoRemovalReason;
        this.screenShareRemovalReason = videoRemovalReason;
        await this.disableVideoMedia(previousUpdateStatus);
        await this.videoFilterManager?.stop();
    }
    async removeScreenShareVideo(reason) {
        if (!this.localScreenShare.transceiver) {
            return;
        }
        let previousMediaUpdateStatus = this.mediaUpdateStatus;
        this.mediaUpdateStatus = MediaUpdateStatus.REMOVING_SCREEN_SHARE;
        this.screenShareRemovalReason = reason;
        await this.disableLocalMedia(this.localScreenShare, previousMediaUpdateStatus);
    }
    async disableVideoMedia(mediaUpdateStatus) {
        for (const localMedia of [this.localCameraVideo, this.localScreenShare]) {
            localMedia.active = false;
            await this.disableTransceiver(localMedia);
        }
        this.unpublishVideo(mediaUpdateStatus);
    }
    async disableLocalMedia(media, mediaUpdateStatus) {
        await this.disableTransceiver(media);
        if (!this.localCameraVideo.active && !this.localScreenShare.active) {
            this.unpublishVideo(mediaUpdateStatus);
        }
        else {
            this.emitVideoRemovedEvent();
            this.mediaUpdateStatus = MediaUpdateStatus.MEDIA_PENDING;
            await this.negotiateVideoPublisher(this.joinedConference || this.joinedDialog);
        }
    }
    unpublishVideo(mediaUpdateStatus) {
        const action = this.joinedConference || this.joinedDialog ? WsAction.UNPUBLISH_VIDEO_CONFERENCE : WsAction.UNPUBLISH_VIDEO_CALL;
        this.gateway.send({
            action: action,
            callId: this.callId
        });
        if (mediaUpdateStatus === MediaUpdateStatus.MEDIA_PENDING) {
            this.emitVideoRemovedEvent();
            this.videoPublisherCleanup();
        }
    }
    async disableTransceiver(media) {
        media.transceiver.sender.track?.stop();
        await media.transceiver.sender.replaceTrack(null);
        media.transceiver.direction = 'inactive';
        media.transceiver = null;
    }
    async replaceTrack(media, track) {
        if (!media.transceiver) {
            return;
        }
        if (media.transceiver.sender.track === track) {
            return;
        }
        media.transceiver.sender.track?.stop();
        await media.transceiver.sender.replaceTrack(track);
    }
    throwMediaError(error) {
        switch (error.name) {
            case ("NotAllowedError"):
                throw ApplicationErrorCode.DEVICE_FORBIDDEN;
            case ("NotFoundError"):
                throw ApplicationErrorCode.DEVICE_NOT_FOUND;
            case ("NotReadableError"):
                throw ApplicationErrorCode.DEVICE_UNAVAILABLE;
            default:
                throw error;
        }
    }
    getDurationInSeconds(currentTime) {
        return Math.floor((currentTime.getTime() - this.callEstablishTime.getTime()) / 1000);
    }
    onIceCandidate(action = WsAction.ICE_CANDIDATE, event, type = undefined) {
        let candidate = event.candidate ? event.candidate : { completed: true };
        this.gateway.send({ action: action, callId: this.callId, type: type, ice: candidate });
    }
    emitEstablished(audioStream) {
        this.remoteAudioStream = audioStream;
        this.callStatus = CallStatus.ESTABLISHED;
        this.callEstablishTime = new Date();
        this.apiEventEmitter.emit(CallsApiEvent.ESTABLISHED, {
            stream: this.remoteAudioStream
        });
    }
    emitEarlyMedia(audioStream) {
        this.remoteAudioStream = audioStream;
        this.apiEventEmitter.emit(CallsApiEvent.EARLY_MEDIA, {
            stream: this.remoteAudioStream
        });
    }
    onAudioPcTrack(event) {
        if (this.isEarlyMedia) {
            this.emitEarlyMedia(event.streams[0]);
        }
        else if (this.callStatus !== CallStatus.ESTABLISHED) {
            this.emitEstablished(event.streams[0]);
        }
    }
    scheduleHangup(hangupEvent) {
        const timeout = setTimeout(() => {
            this.eventEmitter.emit('hangup', { status: hangupEvent.status });
        }, 3000);
        this.eventEmitter.once(WsEvent.HANGUP, event => clearTimeout(timeout));
    }
    createVideoPublisherPC(isConference) {
        this.videoPublisherPC = MonitoredPeerConnection.create(this.rtcConfig, this.callId, PeerConnectionTag.VideoPublisher, this.conferenceId || this.dialogId, MediaType.VIDEO, this.logger, this.gateway);
        let action = isConference ? WsAction.ICE_CANDIDATE_VIDEO_CONFERENCE : WsAction.ICE_CANDIDATE_VIDEO_CALL;
        this.videoPublisherPC.peerConnection.onicecandidate = ice => this.onIceCandidate(action, ice, 'publisher');
        this.videoPublisherPC.peerConnection.onconnectionstatechange = () => this.onVideoPublisherConnectionStateChanged();
    }
    handlePublishVideoFlowError(error) {
        this.logger.error(`Publish video flow error occurred: ${error}`, this.callId);
        this.videoPublisherCleanup();
        let errorCode = HangupStatusFactory.getApplicationHangupStatus(error);
        this.apiEventEmitter.emit(CallsApiEvent.ERROR, { errorCode });
    }
    handleGetUserMediaError(error, userMediaErrorType) {
        this.logger.error(`Get user media flow error occurred: ${error}`, this.callId);
        if (!this.hasCameraVideo()) {
            this.videoPublisherCleanup();
        }
        else if (userMediaErrorType === "screen-share") {
            this.localScreenShare.active = false;
            this.mediaUpdateStatus = MediaUpdateStatus.IDLE;
        }
        let errorCode = HangupStatusFactory.getApplicationHangupStatus(error);
        this.apiEventEmitter.emit(CallsApiEvent.ERROR, { errorCode });
    }
    getCameraVideoStream(cameraOrientation = CameraOrientation.FRONT, useExactDevice = true) {
        return this.device.getLocalStream(false, true, cameraOrientation, true, useExactDevice, this.applicationCallOptions?.videoOptions?.cameraVideoFrameRate);
    }
    async getScreenShareVideoStream(displayOptions) {
        if (displayOptions) {
            this.checkDisplaySurfaceSupported();
        }
        const stream = await this.device.getDisplayMedia(displayOptions, this.applicationCallOptions?.videoOptions?.screenShareFrameRate);
        if (displayOptions) {
            this.checkAllowedDisplaySurface(stream, displayOptions);
        }
        return this.setScreenShareInactive(stream);
    }
    checkAllowedDisplaySurface(stream, displayOptions) {
        stream.getVideoTracks().forEach(track => {
            const isSharedScreenAllowed = displayOptions.allowedDisplayOptions.includes(track.getCapabilities().displaySurface);
            if (!isSharedScreenAllowed) {
                RTCMediaDevice.closeMediaStream(stream);
                throw ApplicationErrorCode.FORBIDDEN_DISPLAY_SURFACE;
            }
        });
    }
    checkDisplaySurfaceSupported() {
        if (!("displaySurface" in navigator.mediaDevices.getSupportedConstraints())) {
            throw ApplicationErrorCode.SCREEN_SHARING_CONTROL_UNSUPPORTED;
        }
    }
    setScreenShareInactive(stream) {
        stream.getVideoTracks()[0].onended = () => this.screenShare(false);
        return stream;
    }
    onNetworkQualityStatisticsChanged(networkQualityStatistics, currentMediaStats) {
        this.networkQualityMonitor.observe(networkQualityStatistics);
        if (this.networkQualityMonitor.localNetworkChanged() && this.callStatus === CallStatus.ESTABLISHED) {
            let currentQualityStatistics = this.networkQualityMonitor.current();
            currentMediaStats.mos = currentQualityStatistics.mos;
            this.emitNetworkQualityChanged({
                networkQuality: this.currentNetworkQuality(),
                currentMediaStats: currentMediaStats
            });
        }
    }
    emitNetworkQualityChanged(event) {
        this.apiEventEmitter.emit(CallsApiEvent.NETWORK_QUALITY_CHANGED, event);
        this.gateway.send({
            action: WsAction.NETWORK_QUALITY_CHANGED,
            callId: this.callId,
            networkQuality: event.networkQuality,
        });
    }
    currentNetworkQuality() {
        return this.networkQualityMonitor.current().networkQuality;
    }
    videoPublisherCleanup(isMigration = false) {
        if (!isMigration) {
            for (let media of [this.localCameraVideo, this.localScreenShare]) {
                if (media) {
                    media.active = false;
                    media.transceiver?.sender.track?.stop();
                    if (this.videoPublisherPC?.peerConnection.connectionState !== "closed") {
                        media.transceiver?.stop();
                    }
                    media.transceiver = null;
                }
            }
            this.localCameraVideoStream?.getVideoTracks().forEach(track => track.stop());
            this.localScreenShareStream?.getVideoTracks().forEach(track => track.stop());
            this.mediaUpdateStatus = MediaUpdateStatus.IDLE;
        }
        if (this.videoPublisherPC) {
            this.videoPublisherPC.close();
            this.videoPublisherPC = null;
        }
    }
    loadParticipant(rawParticipant) {
        const participant = new Participant();
        Object.assign(participant, rawParticipant);
        participant.media = Object.assign(new Media(Object.assign(new Audio(), participant.media.audio), Object.assign(new Video(), participant.media.video)), participant.media);
        participant.endpoint = this.getEndpoint(participant.endpoint);
        participant.role = this.getRole(participant.role);
        return participant;
    }
    getEndpoint(rawEndpoint) {
        switch (rawEndpoint.type) {
            case (EndpointType.PHONE):
                return Object.assign(new PhoneEndpoint(), rawEndpoint);
            case (EndpointType.WEBRTC):
                return Object.assign(new WebrtcEndpoint(), rawEndpoint);
            case (EndpointType.VIBER):
                return Object.assign(new ViberEndpoint(), rawEndpoint);
            case (EndpointType.WHATSAPP):
                return Object.assign(new WhatsAppEndpoint(), rawEndpoint);
            case (EndpointType.WEBSOCKET):
                return Object.assign(new WebsocketEndpoint(), rawEndpoint);
            default:
                return Object.assign(new SipEndpoint(), rawEndpoint);
        }
    }
    getRole(rawRole) {
        const type = rawRole?.type;
        if (!type) {
            return null;
        }
        switch (type) {
            case (RoleType.LISTENER):
                return Object.assign(new ListenerRole(), rawRole);
            case (RoleType.MONITOR):
                return Object.assign(new MonitorRole(), rawRole);
            case (RoleType.ADVISOR):
                return Object.assign(new AdvisorRole(), rawRole);
            case (RoleType.AUDITOR):
                return Object.assign(new AuditorRole(), rawRole);
            default:
                return Object.assign(new DefaultRole(), rawRole);
        }
    }
    sendDTMFInfo(dtmf, toneDuration) {
        this.gateway.send({
            action: WsAction.DTMF_INFO,
            callId: this.callId,
            digit: dtmf,
            duration: toneDuration
        });
    }
    emitVideoRemovedEvent() {
        if (this.mediaUpdateStatus === MediaUpdateStatus.REMOVING_CAMERA_VIDEO) {
            this.apiEventEmitter.emit(CallsApiEvent.CAMERA_VIDEO_REMOVED, { reason: this.cameraVideoRemovalReason });
            this.cameraVideoRemovalReason = null;
        }
        else if (this.mediaUpdateStatus === MediaUpdateStatus.REMOVING_SCREEN_SHARE) {
            this.apiEventEmitter.emit(CallsApiEvent.SCREEN_SHARE_REMOVED, { reason: this.screenShareRemovalReason });
            this.screenShareRemovalReason = null;
        }
        else if (this.mediaUpdateStatus === MediaUpdateStatus.REMOVING_VIDEO) {
            this.apiEventEmitter.emit(CallsApiEvent.CAMERA_VIDEO_REMOVED, { reason: this.cameraVideoRemovalReason });
            this.cameraVideoRemovalReason = null;
            this.apiEventEmitter.emit(CallsApiEvent.SCREEN_SHARE_REMOVED, { reason: this.screenShareRemovalReason });
            this.screenShareRemovalReason = null;
        }
    }
    emitVideoAddedEvent() {
        if (this.mediaUpdateStatus === MediaUpdateStatus.ADDING_CAMERA_VIDEO) {
            this.apiEventEmitter.emit(CallsApiEvent.CAMERA_VIDEO_ADDED, { stream: this.localCameraVideoStream });
        }
        else if (this.mediaUpdateStatus === MediaUpdateStatus.ADDING_SCREEN_SHARE) {
            this.apiEventEmitter.emit(CallsApiEvent.SCREEN_SHARE_ADDED, { stream: this.localScreenShareStream });
        }
    }
    resolveCurrentCallState() {
        if (this.joinedDialog) {
            return ApplicationCallState.IN_DIALOG;
        }
        else if (this.joinedConference) {
            return ApplicationCallState.IN_CONFERENCE;
        }
        else {
            return ApplicationCallState.STANDALONE;
        }
    }
    resolveCallStateAfterReconnect(event) {
        if (event.dialogState) {
            return ApplicationCallState.IN_DIALOG;
        }
        else if (event.conferenceState) {
            return ApplicationCallState.IN_CONFERENCE;
        }
        else {
            return ApplicationCallState.STANDALONE;
        }
    }
    async handleApplicationCallChanges(event) {
        if (!event.callState) {
            return;
        }
        this.handleCallRecordingTypeChangeOnCallReconnected(event.callState);
    }
    async handleCallReconnectedToSameState(stateAfterReconnect, event) {
        if (stateAfterReconnect === ApplicationCallState.IN_CONFERENCE) {
            if (event.conferenceState.id !== this.conferenceId) {
                if (this.recordingState().conferenceRecording !== "UNDEFINED") {
                    this.handleConferenceRecordingStopped({ id: this.conferenceId });
                }
                await this.handleLeftApplicationConference({ status: ApplicationErrorCode.NORMAL_HANGUP });
                this.handleJoinedApplicationConference(event.conferenceState);
            }
            else {
                this.handleParticipantChangesOnCallReconnected(event.conferenceState.participants);
                this.handleConferenceRecordingTypeChangeOnCallReconnected(event.conferenceState);
            }
        }
        if (stateAfterReconnect === ApplicationCallState.IN_DIALOG) {
            if (event.dialogState.id !== this.dialogId) {
                if (this.recordingState().dialogRecording !== "UNDEFINED") {
                    this.handleDialogRecordingStopped({ id: this.dialogId });
                }
                await this.handleDialogFinished({ id: this.dialogId, status: ApplicationErrorCode.NORMAL_HANGUP });
                this.handleDialogEstablished(event.dialogState);
            }
            else {
                this.handleParticipantChangesOnCallReconnected(event.dialogState.participants);
                this.handleDialogRecordingTypeChangeOnCallReconnected(event.dialogState);
            }
        }
    }
    async handleCallReconnectedToNewState(stateBeforeReconnect, stateAfterReconnect, event) {
        if (stateBeforeReconnect === ApplicationCallState.IN_DIALOG) {
            if (this.recordingState().dialogRecording !== "UNDEFINED") {
                this.handleDialogRecordingStopped({ id: this.dialogId });
            }
            await this.handleDialogFinished({ id: this.dialogId, status: ApplicationErrorCode.NORMAL_HANGUP });
        }
        else if (stateBeforeReconnect === ApplicationCallState.IN_CONFERENCE) {
            if (this.recordingState().conferenceRecording !== "UNDEFINED") {
                this.handleConferenceRecordingStopped({ id: this.conferenceId });
            }
            await this.handleLeftApplicationConference({ status: ApplicationErrorCode.NORMAL_HANGUP });
        }
        if (stateAfterReconnect === ApplicationCallState.IN_CONFERENCE) {
            this.handleJoinedApplicationConference(event.conferenceState);
            await this.handleJoinedVideoConference();
        }
        else if (stateAfterReconnect === ApplicationCallState.IN_DIALOG) {
            this.handleDialogEstablished(event.dialogState);
            await this.handleJoinedVideoConference();
        }
        else if (stateAfterReconnect === ApplicationCallState.STANDALONE) {
            this.videoCallJoinedHandler();
        }
    }
    handleCallRecordingTypeChangeOnCallReconnected(callState) {
        callState.recordingType = callState.recordingType ?? "UNDEFINED";
        if (this.recordingState().callRecording !== callState.recordingType) {
            if (this.recordingState().callRecording && callState.recordingType === "UNDEFINED") {
                this.handleCallRecordingStopped({ callId: this.callId });
            }
            else if (callState.recordingType !== "UNDEFINED") {
                this.handleCallRecordingStarted({ callId: this.callId, recordingType: callState.recordingType });
            }
        }
    }
    handleDialogRecordingTypeChangeOnCallReconnected(dialogState) {
        dialogState.recordingType = dialogState.recordingType ?? "UNDEFINED";
        if (this.recordingState().dialogRecording !== dialogState.recordingType) {
            if (this.recordingState().dialogRecording && dialogState.recordingType === "UNDEFINED") {
                this.handleDialogRecordingStopped({ id: dialogState.id });
            }
            else if (dialogState.recordingType !== "UNDEFINED") {
                this.handleDialogRecordingStarted({ id: dialogState.id, recordingType: dialogState.recordingType });
            }
        }
    }
    handleConferenceRecordingTypeChangeOnCallReconnected(conferenceState) {
        conferenceState.recordingType = conferenceState.recordingType ?? "UNDEFINED";
        if (this.recordingState().conferenceRecording !== conferenceState.recordingType) {
            if (this.recordingState().conferenceRecording && conferenceState.recordingType === "UNDEFINED") {
                this.handleConferenceRecordingStopped({ id: conferenceState.id });
            }
            else if (conferenceState.recordingType !== "UNDEFINED") {
                this.handleConferenceRecordingStarted({
                    id: conferenceState.id,
                    recordingType: conferenceState.recordingType
                });
            }
        }
    }
    handleParticipantChangesOnCallReconnected(participants) {
        let participantsBeforeReconnect = this.participants;
        let participantsAfterReconnect = this.createParticipantMap(participants);
        this.participants = participantsAfterReconnect;
        const allIdentities = new Set([
            ...Object.keys(participantsBeforeReconnect), ...Object.keys(participantsAfterReconnect)
        ]);
        allIdentities.forEach(identity => {
            let participantBeforeReconnect = participantsBeforeReconnect[identity];
            let participantAfterReconnect = participantsAfterReconnect[identity];
            if (identity === this.currentUserIdentity) {
                this.handleLocalParticipantChangesDuringReconnect(participantBeforeReconnect, participantAfterReconnect);
                return;
            }
            if (participantBeforeReconnect && !participantAfterReconnect) {
                this.handleParticipantLeftDuringReconnect(participantBeforeReconnect);
            }
            else if (!participantBeforeReconnect && participantAfterReconnect) {
                this.handleParticipantJoinedDuringReconnect(participantAfterReconnect);
            }
            else {
                this.handleParticipantChangesDuringReconnect(participantBeforeReconnect, participantAfterReconnect);
            }
        });
    }
    async renegotiateMedia(iceRestart) {
        this.gateway.send({
            action: WsAction.RENEGOTIATE_MEDIA,
            videoPublisherOffer: iceRestart ? await this.videoPublisherPC?.restartIce() : null,
            iceRestart,
        });
    }
    handleParticipantLeftDuringReconnect(participant) {
        this._dataChannel?.removeParticipant(participant.endpoint.identifier);
        this.apiEventEmitter.emit(CallsApiEvent.PARTICIPANT_LEFT, { participant: participant });
    }
    handleParticipantJoinedDuringReconnect(participant) {
        this._dataChannel?.addParticipant(participant.endpoint.identifier);
        const event = participant.state === State.JOINING
            ? CallsApiEvent.PARTICIPANT_JOINING : CallsApiEvent.PARTICIPANT_JOINED;
        this.apiEventEmitter.emit(event, { participant: participant });
    }
    handleParticipantChangesDuringReconnect(participantBeforeReconnect, participantAfterReconnect) {
        if (participantBeforeReconnect.state !== participantAfterReconnect.state) {
            if (participantAfterReconnect.state === State.JOINED) {
                this.apiEventEmitter.emit(CallsApiEvent.PARTICIPANT_JOINED, { participant: participantAfterReconnect });
            }
            else {
                this.apiEventEmitter.emit(CallsApiEvent.PARTICIPANT_LEFT, { participant: participantAfterReconnect });
                this.apiEventEmitter.emit(CallsApiEvent.PARTICIPANT_JOINING, { participant: participantAfterReconnect });
            }
        }
        if (participantBeforeReconnect.media.audio.muted !== participantAfterReconnect.media.audio.muted) {
            this.emitMutedEvent(participantAfterReconnect);
        }
        if (participantBeforeReconnect.media.audio.deaf !== participantAfterReconnect.media.audio.deaf) {
            this.emitDeafEvent(participantAfterReconnect);
        }
        if (participantBeforeReconnect.media.video.blind !== participantAfterReconnect.media.video.blind) {
            this.emitBlindEvent(participantAfterReconnect);
        }
        if (participantBeforeReconnect.disconnected !== participantAfterReconnect.disconnected) {
            this.emitDisconnectedEvent(participantAfterReconnect);
        }
        if (participantBeforeReconnect.role !== participantAfterReconnect.role) {
            this.emitParticipantRoleChanged(participantAfterReconnect);
        }
        this.emitParticipantTalkingEvent(participantAfterReconnect, false);
    }
    handleLocalParticipantChangesDuringReconnect(participantBeforeReconnect, participantAfterReconnect) {
        if (participantBeforeReconnect.role !== participantAfterReconnect.role) {
            if (!this.isRoleMediaAllowed()) {
                this.disableMedia();
            }
            this.apiEventEmitter.emit(CallsApiEvent.ROLE_CHANGED, { role: participantAfterReconnect.role });
        }
    }
    emitTalkingWhileMutedEvent() {
        this.apiEventEmitter.emit(CallsApiEvent.TALKING_WHILE_MUTED, {});
    }
    handleMessageReceived(event) {
        this.apiEventEmitter.emit(CallsApiEvent.MESSAGE_RECEIVED, {
            message: event.message
        });
    }
    handleMachineDetectionFinished(event) {
        this.updateCustomData(event);
        this.apiEventEmitter.emit(CallsApiEvent.MACHINE_DETECTION_FINISHED, {
            detectionResult: event.detectionResult,
            confidenceRating: event.confidenceRating,
        });
    }
    handleMachineDetectionFailed(event) {
        this.updateCustomData(event);
        this.apiEventEmitter.emit(CallsApiEvent.MACHINE_DETECTION_FAILED, {
            reason: event.reason
        });
    }
    updateCustomData(event) {
        if (this.applicationCallOptions && event.customData) {
            this.applicationCallOptions.customData = {
                ...this.applicationCallOptions.customData,
                ...event.customData,
            };
        }
    }
}
//# sourceMappingURL=DefaultApplicationCall.js.map