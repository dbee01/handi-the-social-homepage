import VideoType from "../../../util/VideoType";
import { Logger } from "../../../log/Logger";
export declare const BitrateConfig: {
    [key in VideoType]: number;
};
export declare function configureForSending(peerConnection: RTCPeerConnection, streamKind: "audio" | "video", bitrateBps: number, logger: Logger): void;
