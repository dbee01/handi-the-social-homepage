export declare class EmptyAudioStream {
    private readonly audioContext;
    private readonly oscillator;
    private readonly mediaStreamDestination;
    constructor();
    stream(): MediaStream;
    close(): void;
}
