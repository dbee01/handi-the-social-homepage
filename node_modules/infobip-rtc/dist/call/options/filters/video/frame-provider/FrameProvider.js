import { FrameProviderMediaStreamTrackProcessor } from './FrameProviderMediaStreamTrackProcessor';
import { FrameProviderProxyVideo } from './FrameProviderProxyVideo';
export function createFrameProvider(track, fps, frameListener) {
    if (typeof MediaStreamTrackProcessor === 'function') {
        console.log('MediaStreamTrackProcessor is available!');
        return new FrameProviderMediaStreamTrackProcessor(track, fps, frameListener);
    }
    else {
        console.log('MediaStreamTrackProcessor is not available...');
        return new FrameProviderProxyVideo(track, fps, frameListener);
    }
}
//# sourceMappingURL=FrameProvider.js.map