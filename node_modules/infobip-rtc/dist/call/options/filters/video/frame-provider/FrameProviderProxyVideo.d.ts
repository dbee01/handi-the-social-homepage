import { FrameListener, FrameProvider } from './FrameProvider';
export declare class FrameProviderProxyVideo implements FrameProvider {
    private readonly track;
    private fps;
    private readonly frameListener;
    private video;
    constructor(track: MediaStreamTrack, fps: number, frameListener: FrameListener);
    configureFramerate(filterFps: number): void;
    start(): void;
    private startAsyncFrameProvision;
    private getVideoElement;
    destroy(): void;
}
