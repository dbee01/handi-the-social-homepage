export function getRecordingOptions(options) {
    const recordingType = options?.recordingOptions?.recordingType;
    if (recordingType && recordingType !== "UNDEFINED") {
        return {
            recordingOptions: JSON.stringify(options?.recordingOptions)
        };
    }
    return null;
}
export function getMachineDetectionOptions(options) {
    const machineDetectionEnabled = options?.machineDetectionOptions?.enabled;
    if (machineDetectionEnabled) {
        return {
            machineDetectionOptions: JSON.stringify(options?.machineDetectionOptions.toJSON())
        };
    }
    return null;
}
//# sourceMappingURL=CallOptionsUtil.js.map