node_modules/
.env
.gtfs-cache/
data/
*.log
log.txt

# Offline AI model files — fetched locally via scripts/fetch-ai-assets.sh
# (not committed: ~130MB ONNX model; runtime/wasm in js/vendor stays in git)
models/
twa/*.keystore

# TWA build artifacts (keystore, APK/AAB) — deployed via rsync only
pwa/

# Zed editor local settings
.zed/
