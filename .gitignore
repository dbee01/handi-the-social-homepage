node_modules/
.env
.gtfs-cache/
data/
package-lock.json
*.log
log.txt

# Offline AI model files — fetched locally via scripts/fetch-ai-assets.sh
# (not committed: ~130MB ONNX model; runtime/wasm in js/vendor stays in git)
models/
twa/*.keystore
pwa/*.keystore
